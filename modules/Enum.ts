// Enum
enum Membership {
  Simple,
  Standart,
  Premium,
}
const membership = Membership.Simple;
const membershipReverse = Membership[0];
enum SocialMedia {
  vk = 'Vk',
  telegram = 'Telegram',
  instagram = 'Instagram',
}
